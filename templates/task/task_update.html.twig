{% extends "base.html.twig" %}

{% block body %}
    <br><br><br><br><br>

    <h3>Editar Tarea</h3>
    {{ form_start(newTaskForm, {'attr': {'novalidate': 'novalidate'}})}}
        {{ form_widget(newTaskForm)}}
        <button class="btn btn-primary btn-lg" type="submit" formmethod="post"><i class="fa fa-save"></i> Guardar</button>
    {{ form_end(newTaskForm) }}
    <br>
    
{% endblock %}