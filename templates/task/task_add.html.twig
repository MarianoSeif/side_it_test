{% extends "base.html.twig" %}

{% block title %} Agregar Taera {% endblock %}
{% block body %}
    <h3>Agregar Tarea</h3>
    {{ form_start(newTaskForm, {'attr': {'novalidate': 'novalidate'}})}}
        {{ form_widget(newTaskForm)}}
        <button class="btn btn-primary btn-lg" type="submit" formmethod="post"><i class="fa fa-save"></i> Agregar</button>
    {{ form_end(newTaskForm) }}
    <br>
{% endblock %}